@model IEnumerable<Teatr.Models.Przedstawienie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Scena.nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tytul)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.data_rozp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.data_zak)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Scena.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tytul)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data_rozp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data_zak)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.przedstawienie_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.przedstawienie_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.przedstawienie_id })
        </td>
    </tr>
}

</table>
